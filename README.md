# 🏃 超慢跑計時器

一個簡約深色風格的超慢跑計時器 Web App，具有節拍器音效、動畫效果，並整合 Google Sheets 記錄跑步歷史。

## ✨ 功能特色

- 🔐 登入驗證保護
- ⏱️ 可調整的跑步時間（5 分鐘間隔）
- 🎵 步頻節拍器音效（10 BPM 間隔）
- 💫 脈動動畫隨步頻跳動
- ⏸️ 暫停 / 繼續功能
- 🎉 完成慶祝音效
- 📊 歷史紀錄長條圖（可切換週次）
- 📱 響應式設計（Mobile First）

## 🚀 快速開始

### 1. 安裝依賴

```bash
npm install
```

### 2. 設定環境變數

複製 `.env.example` 為 `.env`：

```bash
cp .env.example .env
```

編輯 `.env` 檔案：

```env
# Google Apps Script 部署後的網址
VITE_GOOGLE_APP_SCRIPT_URL=https://script.google.com/macros/s/你的腳本ID/exec

# 登入帳號密碼
VITE_USERNAME=你的使用者名稱
VITE_PASSWORD=你的密碼
```

### 3. 啟動開發伺服器

```bash
npm run dev
```

開啟 http://localhost:5173 即可使用。

---

## 📊 Google Sheets 設定

### 步驟一：建立 Google Sheets

1. 前往 [Google Sheets](https://sheets.google.com)
2. 建立新的試算表，命名為「超慢跑記錄」

### 步驟二：建立工作表

建立以下兩個工作表（點擊底部的 + 號新增）：

#### 工作表 1：`Record`

| ID | DateTime | Duration |
|----|----------|----------|
| 1  | 2025-12-09T10:30:00.000Z | 10 |
| 2  | 2025-12-09T14:15:30.000Z | 15 |

- **ID**：自動遞增的編號
- **DateTime**：跑步日期時間（ISO 8601 格式）
- **Duration**：跑步時間（分鐘）

#### 工作表 2：`TodayDuration`

| Date | TotalMinutes |
|------|--------------|
| 2025-12-09 | 25 |

- **Date**：日期（YYYY-MM-DD 格式）
- **TotalMinutes**：該日跑步總時間（分鐘，由系統自動加總）

> ⚠️ **重要**：工作表名稱必須完全符合 `Record` 和 `TodayDuration`（區分大小寫）

---

## 🔧 Google Apps Script 設定

### 步驟一：開啟 Apps Script 編輯器

1. 在 Google Sheets 中，點擊選單：**擴充功能** > **Apps Script**
2. 這會開啟 Apps Script 編輯器

### 步驟二：貼上程式碼

1. 刪除編輯器中的預設程式碼
2. 複製 `google-apps-script.js` 的全部內容
3. 貼上到編輯器中
4. 點擊 💾 **儲存**（或按 Ctrl+S）
5. 將專案命名為「超慢跑計時器 API」

### 步驟三：部署為網頁應用程式

1. 點擊右上角的 **部署** > **新增部署**
2. 點擊「選取類型」旁的 ⚙️ 齒輪，選擇 **網頁應用程式**
3. 設定以下選項：
   - **說明**：超慢跑計時器 API v1
   - **執行身分**：我
   - **誰可以存取**：所有人
4. 點擊 **部署**
5. 點擊 **授權存取**，選擇你的 Google 帳戶
6. 如果出現「這個應用程式未經驗證」：
   - 點擊 **進階**
   - 點擊 **前往「超慢跑計時器 API」（不安全）**
   - 點擊 **允許**
7. 複製顯示的 **網頁應用程式網址**

### 步驟四：設定環境變數

將複製的網址貼到 `.env` 檔案的 `VITE_GOOGLE_APP_SCRIPT_URL`：

```env
VITE_GOOGLE_APP_SCRIPT_URL=https://script.google.com/macros/s/AKfycb.../exec
```

---

## 🔄 更新 Apps Script

如果需要更新 Apps Script 程式碼：

1. 修改程式碼後儲存
2. 點擊 **部署** > **管理部署**
3. 點擊右上角的 ✏️ **編輯**
4. 版本選擇 **新版本**
5. 點擊 **部署**

> 💡 更新後網址不會改變，不需要重新設定環境變數

---

## 📁 專案結構

```
slow-jogging-v3/
├── src/
│   ├── components/
│   │   ├── LoginPage.jsx      # 登入頁面
│   │   ├── TimerSetup.jsx     # 計時設定
│   │   ├── RunningView.jsx    # 計時運作
│   │   └── HistoryView.jsx    # 歷史紀錄
│   ├── utils/
│   │   ├── helpers.js         # 音效、時間工具
│   │   └── api.js             # API 呼叫
│   ├── App.jsx                # 主應用程式
│   ├── main.jsx               # 進入點
│   └── index.css              # 樣式
├── .env.example               # 環境變數範本
├── google-apps-script.js      # GAS 程式碼
└── index.html
```

---

## 🚀 自動部署到 GitHub Pages

本專案已配置 GitHub Actions，推送到 `main` 分支時會自動部署到 GitHub Pages。

> [!IMPORTANT]
> **重要注意事項**
> 由於 `vite.config.js` 中的 `base` 路徑設定為 `/slow-jogging/`，您的 GitHub Repository 名稱 **必須** 為 `slow-jogging`，否則部署後的網頁會無法正確載入資源。
> 如果您想使用其他專案名稱，請記得修改 `vite.config.js` 中的 `base` 設定。

### 步驟一：建立 GitHub Repository

1. 在 GitHub 建立新的 repository，名稱建議為 `slow-jogging`
2. 將本地專案推送到 GitHub：

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的帳號/slow-jogging.git
git push -u origin main
```

### 步驟二：設定 Repository Secrets

為了讓 GitHub Actions 能正確建置專案，您需要在 Repository 設定中加入環境變數。

1. 進入 GitHub repository 頁面
2. 點擊 **Settings** > **Secrets and variables** > **Actions**
3. 點擊 **New repository secret**，新增以下三個 Secrets（請參考 `.env.example`）：

| Secret 名稱 | 說明 | 範例值 |
|-------------|------|--------|
| `VITE_GOOGLE_APP_SCRIPT_URL` | Google Apps Script 部署後的網址 | `https://script.google.com/macros/s/.../exec` |
| `VITE_USERNAME` | 登入使用者名稱 | `admin` |
| `VITE_PASSWORD` | 登入密碼 | `password123` |

### 步驟三：啟用 GitHub Pages

1. 進入 **Settings** > **Pages**
2. **Source** 選擇 **GitHub Actions** (原本可能是 Deploy from a branch，請改為 GitHub Actions)
3. 儲存設定

### 步驟四：觸發部署

推送任何變更到 `main` 分支即可自動觸發部署：

```bash
git add .
git commit -m "Update"
git push
```

部署完成後，網站將可在以下網址訪問：

```
https://你的帳號.github.io/slow-jogging/
```

> 💡 您可以在 Repository 的 **Actions** 頁籤查看部署進度和狀態。若是第一次部署，可能需要幾分鐘時間。

---

## 🛠️ 開發指令

```bash
# 開發模式
npm run dev

# 建置生產版本
npm run build

# 預覽生產版本
npm run preview
```

---

## 📝 授權

MIT License
